# search

`useSearchStore` — location search state, backed by `WeatherRepository.searchLocations` (`src/infrastructure/weather`).

- `query`, `suggestions`, `isLoading`, `error`
- `searchLocations(query)`, `clearSuggestions()`

UI (search input, results list) is not built yet - only the store exists so far.
